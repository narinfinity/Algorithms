using System;
using System.Collections.Generic;
using System.Linq;

namespace android {
    class Program {
        static void Main(string[] args) {
            //Console.WriteLine($"{P1.friendly(a: 5819291, b: 2958191)}");

            // var n = 5;
            //  var arr = P2.matrix(n);
            //  for (int i = 0; i < n; i++)
            //      Console.WriteLine($"{string.Join(" ", arr[i])}");

            // var arr = Enumerable.Range(1, 10).ToArray();
            // var x = 3;
            // Console.WriteLine($"Index of {x} is: {P3.find(arr, x)}");

            // var s = "WWWWBWWWWWBBBWWWWWWWWWWWWWWBBBBB";
            // var s1 = "4WB5W3B14W5B";
            // Console.WriteLine($"{P4.encodeOrDecode(s)}");

            // var arr = new [] { "8", "+", "9", "*", "4", "*", "2", "-", "20", "/", "2" };
            // Console.WriteLine($"{P5.calc(arr)} -> {8+9*4*2-20/2}");

            //Console.WriteLine($"{string.Join(", ", L2.calc())}");
        }

    }
}